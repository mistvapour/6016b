# 项目结构说明

## 📁 目录结构

```
6016b/
├── backend/                    # 后端服务
│   ├── main.py                # FastAPI 主应用
│   ├── requirements.txt       # Python 依赖
│   ├── pdf_adapter/           # PDF 处理模块
│   ├── mqtt_adapter/          # MQTT 适配器
│   ├── rag_service/           # RAG 服务
│   ├── cdm_system.py          # CDM 语义系统
│   └── ...
│
├── frontend/                   # 前端应用
│   ├── package.json           # Node.js 依赖
│   ├── src/                   # React 源码
│   ├── public/                # 静态资源
│   └── ...
│
├── 华东师范大学硕_博_士论文LaTex模板_/  # 论文模板
│   ├── main.tex               # LaTeX 主文件
│   ├── chapters/              # 章节
│   ├── abstract/              # 摘要
│   └── ...
│
├── link16_output/             # Link16 处理输出
├── mavlink_output/            # MAVLink 处理输出
├── test_output/               # 测试输出
│
├── install.sh                 # 一键安装脚本 ⭐
├── check_dependencies.sh      # 依赖检查脚本
├── 依赖安装指南.md             # 详细安装说明
├── 功能需求检查报告.md         # 功能检查报告
└── README.md                  # 项目说明
```

---

## 🚀 快速开始

### 首次使用

1. **检查系统环境**
```bash
./check_dependencies.sh
```

2. **一键安装依赖**
```bash
./install.sh
```

3. **启动服务**
```bash
# 终端1: 启动后端
source venv/bin/activate
cd backend && python3 main.py

# 终端2: 启动前端
cd frontend && npm run dev
```

---

## 🛠️ 手动安装

### 后端依赖

```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r backend/requirements.txt
```

### 前端依赖

```bash
cd frontend
npm install
```

### 论文模板依赖

```bash
# LaTeX（可选）
brew install --cask mactex

# Python 工具
pip install matplotlib plantuml requests
```

---

## 📋 核心依赖说明

### 后端依赖

| 包名 | 用途 | 重要程度 |
|-----|------|---------|
| FastAPI | Web框架 | ⭐⭐⭐ |
| PyMuPDF | PDF解析 | ⭐⭐⭐ |
| pdfplumber | PDF表格提取 | ⭐⭐⭐ |
| camelot | PDF表格提取 | ⭐⭐⭐ |
| pandas | 数据处理 | ⭐⭐ |
| numpy | 数值计算 | ⭐⭐ |
| OpenCV | 图像处理 | ⭐ |
| pytesseract | OCR识别 | ⭐ |
| mysql-connector | 数据库连接 | ⭐⭐ |

### 前端依赖

| 包名 | 用途 | 重要程度 |
|-----|------|---------|
| React | UI框架 | ⭐⭐⭐ |
| Vite | 构建工具 | ⭐⭐⭐ |
| Tailwind CSS | 样式框架 | ⭐⭐ |
| Lucide React | 图标库 | ⭐ |
| Recharts | 图表库 | ⭐ |

---

## 🔍 依赖检查

### 运行检查脚本

```bash
./check_dependencies.sh
```

### 手动验证

```bash
# Python 环境
python3 --version
pip3 --version

# Node.js 环境
node --version
npm --version

# 验证后端包
python3 -c "import fastapi, fitz, pdfplumber, camelot; print('OK')"

# 验证前端
cd frontend && npm list --depth=0
```

---

## 🐛 常见问题

### Python 问题

**Q: pip 安装失败？**
```bash
# 升级 pip
python3 -m pip install --upgrade pip

# 使用镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

**Q: OpenCV 安装失败？**
```bash
# macOS 需要先安装系统依赖
brew install opencv
pip install opencv-python
```

**Q: Camelot 安装失败？**
```bash
# 需要系统依赖
brew install ghostscript imagemagick
pip install camelot-py[cv]
```

### Node.js 问题

**Q: npm 安装慢？**
```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

**Q: 权限错误？**
```bash
# 清除缓存
rm -rf node_modules package-lock.json
npm install
```

---

## 📖 文档链接

- **依赖安装指南**: `依赖安装指南.md`
- **功能检查报告**: `功能需求检查报告.md`
- **项目 README**: `README.md`
- **系统架构**: `SYSTEM_ARCHITECTURE_OVERVIEW.md`

---

## ✅ 安装检查清单

- [ ] 已安装 Xcode Command Line Tools
- [ ] 已安装 Python 3.x
- [ ] 已安装 Node.js
- [ ] 后端依赖已安装
- [ ] 前端依赖已安装
- [ ] 运行检查脚本无错误
- [ ] 可以启动后端服务
- [ ] 可以启动前端服务

---

## 🎯 下一步

安装完成后，建议：

1. 阅读 `功能需求检查报告.md` 了解项目功能
2. 查看 `系统架构文档` 了解技术架构
3. 运行测试脚本验证功能
4. 开始开发或使用项目

---

**维护者**: GitHub 6016b Project  
**更新日期**: 2024-01-XX
